name             = "fixentra-ai-proxy"
main             = "worker.js"
compatibility_date = "2024-01-01"

# GEMINI_API_KEY is NOT stored here — set it as an encrypted secret:
#   wrangler secret put GEMINI_API_KEY
